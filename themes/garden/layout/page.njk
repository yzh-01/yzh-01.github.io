{% extends '_layout.njk' %}

{% block content %}
<article class="article g-container">
  <header class="article-header">
    <p class="inner-content-kicker">&gt; STATIC_NOTE / OPEN</p>
    <h1>{{ page.title }}</h1>
  </header>
  <div class="article-body">
    {{ page.content | safe }}
  </div>
</article>
{% endblock %}
