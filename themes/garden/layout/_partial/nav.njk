<nav class="g-nav" id="g-nav">
  <div class="g-nav-inner">
    <a href="{{ url_for('/') }}" class="g-nav-logo">{{ config.author }}</a>
    <ul class="g-nav-links">
      <li><a href="{{ url_for('/') }}" {% if is_home() %}class="active"{% endif %}>home</a></li>
      <li><a href="{{ url_for('archives/') }}" {% if is_archive() or is_tag() or is_category() %}class="active"{% endif %}>archive</a></li>
      <li><a href="{{ url_for('about/') }}" {% if page.path == 'about/index.html' %}class="active"{% endif %}>about</a></li>
    </ul>
  </div>
  <div class="g-scroll-track" aria-hidden="true">
    <span class="g-scroll-progress" id="g-scroll-progress"></span>
  </div>
</nav>
